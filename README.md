# 📚 Learning Journal
Documenting my daily learning journey in Python, data visualization, and data analysis — one topic, one commit at a time.

## 🎯 Purpose
This repo is where I track what I learn every day. Instead of scattered notes, everything lives here — searchable, organized, and visible as a timeline of progress.

## 🗂️ Topics Covered
- **Matplotlib** — data visualization (line charts, bar charts, pie charts, scatter plots, subplots, fill_between)
- **Seaborn** — statistical data visualization built on Matplotlib (line plots, bar plots, count plots, pair plots, histograms, KDE plots, box plots, hue/style grouping, markers, color palettes)
- **Applied EDA** — putting Matplotlib/Seaborn to work on a real dataset (Hotel Bookings): data cleaning, transformation, and chart-driven analysis

## 📅 Progress Log
| Date | Topic | What I Learned |
|------|-------|-----------------|
| 2026-08-20 | Matplotlib | `subplot()` — displaying multiple charts in one figure |
| 2026-08-21 | Seaborn | `lineplot()` basics — hue & style for grouping, custom markers, marker size, line width/style, hiding the legend, loading real datasets (tips) with pandas |
| 2026-08-24 | Seaborn | `scatterplot()` — hue & size to encode a numeric column, sizes range for point scaling, style + custom markers for categorical grouping, alpha for transparency, plotting real datasets (tips) |
| 2026-08-25 | Seaborn | `barplot()` — grouping with hue, dodge for separating bars, order & hue_order for custom positioning, palette styling, estimator (mean/sum) and capsize for error bars, width control |
| 2026-08-25 | Seaborn | `countplot()` — visualizing counts of a single categorical column, hue for sub-grouping, palette and width customization |
| 2026-08-25 | Seaborn | `pairplot()` — visualizing pairwise relationships between numerical variables, off-diagonal scatter plots & diagonal KDE/histograms, hue for categorical grouping, vars/x_vars/y_vars for selecting specific columns |
| 2026-08-27 | Seaborn | `histplot()` — binning a numeric column, hue for sub-grouping, multiple = "stack" vs "layer" for combining groups, element & kde overlay, palette styling; `kdeplot()` — smooth density curves, bw_adjust for smoothing, clip to restrict range, fill vs outline, multiple = "layer", hue_order for custom legend order; `boxplot()` — comparing distributions across categories, orient for horizontal/vertical boxes, hue for sub-grouping, color & saturation styling |
| 2026-09-02 | Pandas / EDA | Worked through the Hotel Bookings dataset (119,390 rows) — data cleaning (missing values, duplicates, invalid entries) and data transformation (adding `Total_Nights`, `Total_Guest` columns) as self-made practice questions |
| 2026-09-03 | Matplotlib & Seaborn | Applied EDA to Hotel Bookings dataset with 9 charts: count plot (bookings by hotel), pie chart (% cancelled), stacked column (cancellations by hotel), bar chart (cancellations by market segment), column chart (avg ADR by hotel), line charts (booking demand & ADR by month), donut chart (bookings by customer type), scatter/strip plot (lead time vs cancellation) |
|2026-09-04	|Matplotlib	plt.subplots |— combined 6 individual charts into one Hotel Booking Dashboard (hotel bookings, cancellation ratio, monthly trend, market segment, waiting list, reservation status) using fig.suptitle(), per-axis ax[row,col].set_title(), and plt.tight_layout()
| 2026-09-05 | Pandas , EDA | Data Science Project -Started a Pizza Sales Data Science project using an Excel dataset. Set up the project structure, loaded and explored the dataset, performed EDA including pizza-wise sales and revenue analysis, Veg vs Non-Veg comparison, unsold pizza analysis, price,sales,revenue distributions, scatter plots, category analysis, rating analysis, and correlation heatmap.|
| 2026-09-06 | Pandas ,EDA | Feature Engineering-Continued the Pizza Sales project with time-based analysis. Converted the `Date` column to datetime and created date-based features such as month, day, and day name. Analyzed daily/monthly sales and sales by day of the week. Started the Feature Engineering stage by creating business-related features such as `Revenue_Per_Pizza`, `Unsold_Rate`, `Total_Production`, `Sales_Rate`, and `Sales_Performance`. |
|2026-09-07 | Pizza Sales | Started the Machine Learning stage of the Pizza Sales project. Defined Sales as the target variable, selected appropriate features, created date-based features, avoided data leakage by excluding variables derived from sales, separated features (X) and target (y), and performed an 80/20 train-test split using train_test_split(). |                                                                                                                 
| 2026-09-08 | Pizza Sales | Completed the Machine Learning stage of the Pizza Sales project. Prepared the dataset for modeling, defined `Sales` as the target variable, selected relevant features, created date-based features, handled categorical variables using One-Hot Encoding, avoided data leakage, performed an 80/20 train-test split, and trained and evaluated regression models including Linear Regression, Decision Tree, and Random Forest using MAE, MSE, RMSE, and R² Score. |
| 2026-09-09 |ML-Machine Learning | Built intuition for LinearRegression in scikit-learn using two contrasting examples: (1) perfect linear data, where the model recovered the exact slope/intercept (with floating-point noise like 1e-15 on the intercept — not a bug, just precision limits); (2) imperfect linear data, where the model fit a least-squares best-fit line instead of passing through every point exactly. Reinforced that X (capital) is always a 2D matrix and y (lowercase) a 1D vector, and that .predict() always needs 2D input. Set this up as an ongoing basics→advanced regression journal (multiple regression, regularization, polynomial fits, gradient descent, etc. to follow).|
2026-09-10 |ML-linear-regression-basics|	Extended the basics notebook with a third example (car price depreciation vs. year) to see a negative slope in action, then introduced train_test_split to properly separate training and test data instead of fitting on the full dataset. Learned test_size as a fraction (e.g. 0.2 = 20% held out) and random_state for reproducible splits. Started reading up on prediction error metrics (MAE, MSE, RMSE) as the next topic. Also hit two real bugs today: a plotting call that graphed a single predicted point instead of the full prediction line, and a pd.DataFrame assignment mistake that accidentally overwrote the pd.DataFrame class itself instead of creating a DataFrame — good reminder to always call it as pd.DataFrame(data), not assign to it.
2026-09-11 |ML-polynomial-regression-under-overfitting | Moved from Linear to Polynomial Regression using PolynomialFeatures + LinearRegression. Ran an underfitting demo (low-degree fit missing real curvature) and an overfitting demo (degree-9 fit on 10 points via train_test_split) — Train MSE 15.24 vs. Test MSE 13,940.62, a clear visual case of overfitting. Next: Ridge/Lasso regularization.
2026-09-16 |ML-Encoders |Label Encoder — collects the unique values and gives each one a number, according to alphabetical order. One Hot Encoder — gives only two values, 1 or 0, and makes a separate column for each element/category.|
2026-09-17 |Mini end to end linear regression |Build a toy dataset in pandas — 7 rows of house data: House_Size, Total_Resource, Age, Location (Pune/Bangalore/Mumbai/Delhi), and Price as the target.,Encode the categorical column — pd.get_dummies on Location with dtype=int, producing four 0/1 columns.,Split features and target — x is everything except Price, y is Price.,Train/test split — test_size=0.2, random_state=42, giving 5 training rows and 2 test rows,Fit LinearRegression, predict on the test set, then two loops printing actual vs predicted and the residuals.,Evaluate — MAE, MSE, R², RMSE from sklearn.metrics.|
2026-09-18 |Logistic Regression | Built out LogisticRegression() from first fit to a full evaluation pipeline (predict vs predict_proba, threshold-based classification, confusion matrix, accuracy/precision/recall/f1) on two datasets — one cleanly separable, one not (accuracy dropped to 0.75, showing a real off-diagonal confusion matrix). Spent most of the session hunting a subtle bug: reused variable names (res, predication) were silently printing stale output from earlier cells instead of the current model's results — fixed by keeping each model iteration's variables distinct | 
2026-09-21|Logisctic Regression Classification metrics | LogisticRegression` on two toy datasets, then evaluated it with a confusion matrix, accuracy, precision, recall and F1.|
2026-09-22|Decision Tree |Built and debugged a Decision Tree classifier from scratch on two toy datasets (salary/credit-score loan approval, and weather/temperature play prediction). Covered the core split criteria — Gini impurity and entropy — and worked through why trees can't handle raw categorical features directly. Hit and fixed a string of real bugs: NameError from calling .fit() unbound, ValueError from unencoded categorical columns, a feature-count mismatch after one-hot encoding vs. label encoding, and a sneaky [[0.30]] typo that silently collapsed two intended inputs into one. Landed on LabelEncoder for the categorical feature, visualized the trained tree with plot_tree, and got clean predictions end-to-end. Next: exploring how this connects to Random Forests.|
## 🛠️ Tools & Tech
-- Python
- Jupyter Notebook
- Matplotlib
- Seaborn
- Pandas

## 📌 How This Repo Works
- Each topic gets its own folder as I add more subjects (Pandas, SQL, etc.)
- Notebooks are updated as I learn more on that topic
- Commit messages describe what was learned that day

## ⭐ Feel free to follow along as this grows!
